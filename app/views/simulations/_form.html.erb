<%= form_for(simulation) do |f| %>
  <% if simulation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(simulation.errors.count, "error") %> prohibited this simulation from being saved:</h2>

      <ul>
      <% simulation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :asset_id %>
    <%= f.text_field :asset_id %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :last_generated_at %>
    <%= f.datetime_select :last_generated_at %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
