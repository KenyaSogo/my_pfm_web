<%= form_for(sum_by_acct_class_setting) do |f| %>
  <% if sum_by_acct_class_setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sum_by_acct_class_setting.errors.count, "error") %> prohibited this sum_by_acct_class_setting from being saved:</h2>

      <ul>
      <% sum_by_acct_class_setting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sum_by_account_class_id %>
    <%= f.text_field :sum_by_account_class_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
